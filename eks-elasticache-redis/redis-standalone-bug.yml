---
apiVersion: redis.redis.opstreelabs.in/v1beta1
kind: Redis
metadata:
  name: redis-standalone-bug
  labels:
    app: bug
spec:
  tolerations:
  - effect: NoSchedule
    key: bug
    value: "true"
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: kubernetes.io/hostname
            operator: In
            values:
            - ip-10-0-3-11.us-east-2.compute.internal

  redisExporter:
    enabled: true
    image: quay.io/opstree/redis-exporter:v1.44.0
    imagePullPolicy: IfNotPresent
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 100m
        memory: 128Mi
  kubernetesConfig:
    image: quay.io/opstree/redis:v7.0.5
    imagePullPolicy: IfNotPresent
    resources:
    redisSecret:
      name: redis-secret-bug
      key: password
  securityContext:
    runAsUser: 1000
    fsGroup: 1000
  storage:
    volumeClaimTemplate:
        spec:
          storageClassName: gp2
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 5Gi

---
apiVersion: v1
kind: Service
metadata:
  name: redis-bug
  labels:
    name: redis-standalone-bug
    app: bug
spec:
  selector:
    app: redis-standalone-bug
  type: ClusterIP
  ports:
    - port: 6379
      protocol: TCP
      name: redis-bug
